# 图片上传目录

通过内容管理台上传的图片会提交到这里（GitHub Contents API 写入）。

- 文件名由前端生成：`<时间戳>-<随机串>-<原名>.jpg`
- 上传前浏览器会自动压缩到最长边 1280px、JPEG
- 单张建议不超过 1.6MB（githubSource 默认上限 2MB）

这个 README 只是为了保留目录结构；如果没有任何图片，目录也不会消失（`.gitkeep` 同理）。
